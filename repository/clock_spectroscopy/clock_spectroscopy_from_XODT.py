"""AUTO-GENERATED stub file - do not edit by hand.

Regenerate with ``scripts/generate_stubs.py``. Every class here mirrors
the name and docstring of a real experiment on a source branch; the
body is a no-op stub so the ARTIQ explorer can list it without any of
the real dependencies.
"""

from repository.stub_experiment import _Stub


class AbsImagingFromXXODTWithShelvingAndClearout(_Stub):
    """
    Clock spectroscopy from dropped XXODT with shelving, clearout, and absorption imaging

    Load into an XXODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecDownFromSingleXODTEvaporatedShelving(_Stub):
    """
    Down beam clock spectroscopy from dropped single XODT with evaporation, shelving and clearout

    Load into an XODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecFromSingleXODTEvaporatedAdiabaticallyCooled(_Stub):
    """
    Clock spectroscopy from dropped single XODT with adiabatic cooling into painted trap

    Load into an XODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecFromSingleXODTEvaporatedShelving(_Stub):
    """
    Clock spectroscopy from dropped single XODT with cooling, shelving and clearout

    Load into an XODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecFromXODT(_Stub):
    """
    Clock spectroscopy from dropped, cooled single XODT

    Load into an XODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecFromXXODT(_Stub):
    """
    Clock spectroscopy from dropped XXODT

    Load into an XXODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """


class ClockSpecFromXXODTWithShelving(_Stub):
    """
    Clock spectroscopy from dropped XXODT with shelving and clearout

    Load into an XXODT, then use the up clock beam for spectroscopy, altering the
    (single-pass) AOM.

    Image the ground state atoms, repump and image the excited state, then image
    once more for background.
    """
