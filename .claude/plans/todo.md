Todo list

Audit for methods / variables unused in the whole codebase. For example, `calculate_frequency_for_first_lmt_pulse` in LMTBase.

Make a new experiment that does a sliced clock spec, but includes the OPLL ramp during the pulses. This should use the LMTBase.
