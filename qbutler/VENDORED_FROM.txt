fb98f5c50c8296df15a2b47522c05b78d3a91c47
github.com/charlesbaynham/qbutler branch kernel-rpc-optimizer (PR #19, stacked
on kernel-optimizer-revival / PR #18) — vendored copy; upstream fixes go to
that repo.
Includes the resident-kernel optimizer loop (_optimizer_kernel_loop): one
kernel call per fix, the kernel pulling each next point from the host
optimizer generator over sync RPC and applying it device-side, so ANY host
strategy (grid, coordinate descent, Bayesian optimization) pays one
compile+upload — no recompilation between points. Also the kernel-driven DAG
fix: prepare_kernel_fix() + fix_state_kernel() fix an entire calibration
dependency tree from within a single kernel (walk decisions host-side via
RPC).
